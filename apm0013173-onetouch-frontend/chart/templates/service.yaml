apiVersion: v1
kind: Service
metadata:
  name: {{ include "onetouch-frontend.fullname" . }}
  labels:
    {{- include "onetouch-frontend.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onetouch-frontend.selectorLabels" . | nindent 4 }}
