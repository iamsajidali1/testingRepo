apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-creator.fullname" . }}
  labels:
    {{- include "one-creator.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }} 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "one-creator.selectorLabels" . | nindent 4 }}
