apiVersion: v1
kind: Service
metadata:
  name: {{ include "css-frontend.fullname" . }}
  labels:
    {{- include "css-frontend.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "css-frontend.selectorLabels" . | nindent 4 }}
