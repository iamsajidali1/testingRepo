export interface SchedulerStatusModel {
  pid: string;
  code: string;
  status: string;
}

export interface SchedulerResponseModel {
  _id: string;
  code: string;
  result: string;
  status: string;
}
