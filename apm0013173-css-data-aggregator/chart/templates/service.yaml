apiVersion: v1
kind: Service
metadata:
  name: {{ include "css-data-aggregator.fullname" . }}
  labels:
    {{- include "css-data-aggregator.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }} 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "css-data-aggregator.selectorLabels" . | nindent 4 }}
