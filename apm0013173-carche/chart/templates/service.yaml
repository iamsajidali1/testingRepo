apiVersion: v1
kind: Service
metadata:
  name: {{ include "carche.fullname" . }}
  labels:
    {{- include "carche.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }} 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "carche.selectorLabels" . | nindent 4 }}
