apiVersion: v1
kind: Service
metadata:
  name: {{ include "paperplane.fullname" . }}
  labels:
    {{- include "paperplane.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }} 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paperplane.selectorLabels" . | nindent 4 }}
