apiVersion: v1
kind: Service
metadata:
  name: {{ include "css-ra-vmanage.fullname" . }}
  labels:
    {{- include "css-ra-vmanage.labels" . | nindent 4 }}
  namespace: {{ .Values.namespace }} 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "css-ra-vmanage.selectorLabels" . | nindent 4 }}
